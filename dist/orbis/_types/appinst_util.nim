type
  OrbisAppInstUtilDelete* {.size: sizeof(cint).} = enum
    ORBIS_APPINSTUTILDELETEPATCH = (1'u shl 0),
    ORBIS_APPINSTUTILDELETEADDCONTEXTRADATA = (1'u shl 1),
    ORBIS_APPINSTUTILDELETETEMPDATA = (1'u shl 2),
    ORBIS_APPINSTUTILDELETEDOWNLOADDATA = (1'u shl 3),
    ORBIS_APPINSTUTILDELETEUSERSSAVEDATA = (1'u shl 4),
    ORBIS_APPINSTUTILDELETEALLUSERSSAVEDATA = (1'u shl 5)

